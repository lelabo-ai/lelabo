[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lelabo"
version = "0.1.0"
description = "Modular research lab for learning algorithms."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Adrien Kegreisz" }]
dependencies = [
    "numpy>=1.26",
    "pyyaml>=6",
    "zstandard>=0.22",
    "ano-optimizer>=1.0.0",
    "tomli>=2; python_version < '3.11'",
]

[project.optional-dependencies]
test = ["pytest>=8", "pyyaml>=6", "numpy>=1.26"]
supervised = [
    "torch>=2.2",
    "torchvision>=0.17",
    "scikit-learn>=1.3",
]
rl = [
    "gymnasium>=0.29",
]
nlp = [
    "transformers>=4.40",
    "datasets>=2.18",
]

[project.scripts]
lelabo = "lelabo.cli.main:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
include = ["lelabo*"]

[tool.setuptools.package-data]
"lelabo.config.assets" = ["*.toml"]
"lelabo.capsule.templates" = ["*.md", "*.toml"]

[tool.pytest.ini_options]
markers = [
    "heavy: opt-in local research audits (warn-only).",
]
