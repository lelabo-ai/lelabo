Utility scripts for analysis and plotting.

Current tools:
- `plot_sweep.py`: curve/sweep plots from `outputs/runs/`.
- `plot_sweep_table.py`: aggregated metric tables.
- `profile_update_rules.py`: profile update-rule runtime, FLOPs, and memory.

These are not part of the training engine API.
